# The USB console of the rig is one Ethernet gadget on the USB-C receptacle.
# dwc2 is built into the kernel and the device tree overlay puts it in
# peripheral mode, so this one module is the whole of the gadget.
g_ether
