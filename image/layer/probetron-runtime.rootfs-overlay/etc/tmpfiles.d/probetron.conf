# The volatile state of one Probetron operation.
# run-probetron.mount of the immutable layer bounds the tmpfs that carries it.
d /run/probetron 0755 root root -
d /run/probetron/uploads 0700 root root -
